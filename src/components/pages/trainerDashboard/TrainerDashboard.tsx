import React from 'react';
import { Container, Typography } from '@mui/material';

export const TraderDashboard: React.FC = () => (
    <Container>
        <Typography variant="h3">Trader Dashboard</Typography>
    </Container>
);

